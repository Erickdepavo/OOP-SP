#include <iostream>
#include <string>
#include <vector>
#include "Pavomon.h"

class BattleMove {
  protected:
    string pavomonType;
    string moveName;
    string moveType;
    string value;
    string limit;
};